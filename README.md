# dmesg_logger

This project is Used to get all the dmesg and save in one file with date & time.  For every boot you need to create on new file otherwise it will  continue in same file.

## Getting started

This project is Used to get all the dmesg and save in one file with date & time.  For every boot you need to create on new file otherwise it will  continue in same file.
